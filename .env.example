# このファイルをコピーして .env を作る
#   Mac/Linux : cp .env.example .env
#   Windows   : copy .env.example .env
#
# .env は .gitignore で除外されている。コミットしないこと。
# ここに書く値はローカル開発用であり、本番想定の秘密情報は入れない。

# === PostgreSQL（docker compose が読む） ===
POSTGRES_USER=torano
POSTGRES_PASSWORD=torano_dev_password
POSTGRES_DB=torano_maki
# ローカルに既存のPostgresがいて 5432 が埋まっている場合は 5433 などに変える
POSTGRES_PORT=5432
ADMINER_PORT=8080

# === バックエンドが読む ===
# バックエンドはコンテナ外（ホスト）で動かすため localhost を指す。
# POSTGRES_PORT を変更した場合はこちらのポート番号も合わせること。
DATABASE_URL=postgresql+psycopg://torano:torano_dev_password@localhost:5432/torano_maki

# === Ollama ===
# 手元で検証するときは localhost、DGX Spark を使うときは DGX の IP に変える
# 例: http://192.168.x.x:11434
OLLAMA_BASE_URL=http://localhost:11434
# TODO: 使用するモデルを決めたら記入する
OLLAMA_MODEL=

# === 埋め込み ===
# TODO: モデルと次元数を決めたら記入する。
# EMBEDDING_DIM は DB の vector(N) と必ず一致させること。
# 不一致のまま起動すると、ベクトル挿入時まで気づけないため注意。
#   候補: intfloat/multilingual-e5-large  -> 1024
#         cl-nagoya/ruri-v3-310m          -> 768
EMBEDDING_MODEL=
EMBEDDING_DIM=
